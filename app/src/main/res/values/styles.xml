<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.SwitchStyle">
        <item name="colorControlActivated">@color/switch_on</item>
        <item name="colorSwitchThumbNormal">@color/switch_off</item>
    </style>

    <style name="tv_normal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>

        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/normalText_sp_14</item>
    </style>

    <style name="tv_small" parent="tv_normal">
        <item name="android:textSize">@dimen/smallText_sp_12</item>
    </style>

    <style name="tv_header" parent="tv_normal">
        <item name="android:textSize">@dimen/subHeader_sp_16</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">end</item>
    </style>

</resources>
